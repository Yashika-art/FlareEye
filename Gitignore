# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
.ipynb_checkpoints/

# OS
.DS_Store
Thumbs.db

# Large generated artifacts (regenerate with the commands in README.md
# instead of committing them -- keeps the repo lightweight).
# Comment these out if you'd rather commit the data/models directly.
data/*.csv
models/*.joblib

# Keep the final demo app and small metrics files tracked
!outputs/flareeye_ai_dashboard.html
!outputs/*.json
